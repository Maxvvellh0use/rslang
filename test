src/styles/colors.js

export const PRIMARY_COLOR = '#7ABB54';
export const PRIMARY_TEXT_COLOR = '#412341';
export const CAPTION_TEXT_COLOR = '#B867B7';
export const WHITE_COLOR = '#FFF22F';
export const NAVIGATION_BACK_COLOR = '#214580';
export const BUTTON_DISABLE_COLOR = '#D445DE';
export const ERROR_COLOR = '#D45563';
export const LINKING_TEXT_COLOR = '#0034FF';

-------------------------------------------------------------------------

src/styles/index.js
import * as colors from './colors';
import * as fontSizes from './fontSizes';
import * as fontWeights from './fontWeights';
import * as margins from './margins';

export {
colors,
fontSizes,
fontWeights,
margins,
};
-------------------------------------------------------------------------

src/elements/Buttons.js

import styled from 'styled-components';

import {
colors, fontSizes, fontWeights,
} from 'src/styles'

export const StyledButton = styled.button`
display: flex;
border: ${props => (props.disabled ? '0px' : '2px solid #434541')};
border-radius: 5px;
background-color: ${props => (props.disabled ? colors.BUTTON_DISABLE_COLOR : colors.PRIMARY_COLOR)};
`;

----------------------------------------------------------------------------

src/elements/index.js

import * as Buttons from './Buttons';

export {
Buttons
};

---------------------------------------------------------------------------

src/components/SomeComponent.js

import React from 'react';

import { StyledButton } from 'src/elements/Buttons';

const SomeComponent = () => (
<div>
<StyledButton>Button Text</StyledButton>
</div>
)
